import assert from'node:assert/strict';import fs from'node:fs';const s=fs.readFileSync(new URL('../module/helpers/market-app.mjs',import.meta.url),'utf8');assert.match(s,/<prose-mirror class=\"sd-market-publish-editor\"[^>]*data-market-rich=\"\$\{name\}\"[^>]*button=\"true\"[^>]*editable=\"true\"[^>]*toggled=\"true\"/);assert.match(s,/data-market-rich-value=\"\$\{name\}\" name=\"\$\{name\}\"/);assert.match(s,/readRich=editor=>/);assert.match(s,/syncRich=editor=>/);assert.match(s,/pm\.innerHTML/);assert.match(s,/for\(const type of\[\"input\",\"change\",\"save\"\]\)/);assert.doesNotMatch(s,/document\.execCommand/);assert.doesNotMatch(s,/data-rich-command/);console.log('SD Market native ProseMirror value-sync regression: OK');
