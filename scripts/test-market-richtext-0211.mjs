import assert from'node:assert/strict';import fs from'node:fs';const s=fs.readFileSync(new URL('../module/helpers/market-app.mjs',import.meta.url),'utf8');assert.match(s,/textArea=\(label,name,value=\"\"\)=>/);assert.match(s,/<textarea name=\"\$\{name\}\"/);assert.match(s,/textArea\(loc\(\"SD\.Market\.ExportDesc\"\),\"description\"/);assert.match(s,/textArea\(loc\(\"SD\.Market\.PatchNotes\"\),\"patchNotes\"/);assert.match(s,/form\.elements\[name\]\?\.value/);assert.doesNotMatch(s,/sd-market-publish-editor/);assert.doesNotMatch(s,/data-market-rich/);console.log('SD Market ordinary description textarea regression: OK');
