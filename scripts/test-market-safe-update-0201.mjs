import assert from'node:assert/strict';import{mergeMarketSettings,compareMarketVersions}from'../module/helpers/market-safe-update.mjs';let r=mergeMarketSettings({a:1,b:1},{a:1,b:2},{a:3,b:1});assert.deepEqual(r.value,{a:3,b:2});assert.deepEqual(r.conflicts,[]);r=mergeMarketSettings({a:1},{a:2},{a:3});assert.equal(r.value.a,2);assert.deepEqual(r.conflicts,['a']);r=mergeMarketSettings({rows:[{id:'a',v:1}]},{rows:[{id:'a',v:2},{id:'local',v:1}]},{rows:[{id:'a',v:1},{id:'remote',v:1}]});assert.deepEqual(r.value.rows,[{id:'a',v:2},{id:'remote',v:1},{id:'local',v:1}]);assert.equal(compareMarketVersions('1.2.0','1.1.9'),1);assert.equal(compareMarketVersions('1.0.0','1.0.0'),0);console.log('SD Market safe-update merge regression: OK');
