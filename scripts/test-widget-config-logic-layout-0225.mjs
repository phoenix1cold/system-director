import assert from "node:assert/strict";
import fs from "node:fs";
const popup=fs.readFileSync(new URL("../module/builder/widget-config-popup.mjs",import.meta.url),"utf8");
const css=fs.readFileSync(new URL("../styles/system.css",import.meta.url),"utf8");
assert.doesNotMatch(popup,/\["logic",\s*"Logic"/);
assert.doesNotMatch(popup,/data-cfg-tab="logic"/);
assert.doesNotMatch(popup,/wcfg-attr-graph-btn|wcfg-number-graph-btn|data-attr-group-graph/);
assert.match(popup,/data-open-sheet-blueprint/);
assert.match(popup,/wcfg-open-widget-designer/);
assert.match(css,/wcfg-shared-blueprint/);
console.log("PASS: per-widget Logic UI is removed and settings link only to shared Sheet Blueprint.");
