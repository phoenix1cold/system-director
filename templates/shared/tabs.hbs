<nav class="sheet-tabs" data-group="sheet">
  {{#each tabs as |tab|}}
  <a class="item tab-link {{tab.cssClass}}"
     data-tab="{{tab.id}}"
     data-group="{{tab.group}}">
    {{localize tab.label}}
  </a>
  {{/each}}
</nav>
